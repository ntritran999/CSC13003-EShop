# 1.

## Tool: Postbot

## Time: 9:34 26/06/2026

## Prompt: 

"# Tài liệu Đặc tả API (API Specification) — EShop > **Mục đích:** Tài liệu này cung cấp danh sách và cách sử dụng các Backend API của hệ thống EShop. > Sinh viên có thể sử dụng Postman, cURL hoặc các công cụ tương tự để tương tác với API nhằm phục vụ quá trình kiểm thử phần mềm. > **Base URL:** `http://localhost:3000` --- ## 1. Authentication (Xác thực) ### 1.1 Đăng ký tài khoản - **Endpoint:** `POST /api/register` - **Body (JSON):** ```json { "name": "Nguyen Van A", "email": "test@domain.com", "password": "Password123!" } ``` - **Phản hồi thành công (200 OK):** `{"message": "User registered successfully", "id": 1}` ### 1.2 Đăng nhập - **Endpoint:** `POST /api/login` - **Body (JSON):** ```json { "email": "test@domain.com", "password": "Password123!" } ``` - **Phản hồi thành công (200 OK):** Trả về chuỗi JWT `token` và thông tin `user`. ### 1.3 Quên mật khẩu (Lấy OTP) - **Endpoint:** `POST /api/forgot-password` - **Body (JSON):** ```json { "email": "test@domain.com" } ``` - **Phản hồi thành công (200 OK):** `{"message": "Mã đặt lại mật khẩu đã được tạo", "resetToken": "123456"}` ### 1.4 Đặt lại mật khẩu - **Endpoint:** `POST /api/reset-password` - **Body (JSON):** ```json { "email": "test@domain.com", "resetToken": "123456", "newPassword": "NewPassword123!" } ``` --- ## 2. Người dùng (Users) *Lưu ý: Các API dưới đây yêu cầu truyền Token ở Header: `Authorization: Bearer <token>`* ### 2.1 Lấy thông tin cá nhân - **Endpoint:** `GET /api/users/me` ### 2.2 Cập nhật hồ sơ cá nhân - **Endpoint:** `PUT /api/users/me` - **Mô tả:** Chỉ cho phép cập nhật thông tin cá nhân cơ bản. - **Body (JSON):** ```json { "name": "Nguyen Van A", "shipping_address": "123 Le Loi, Q1, TP.HCM", "phone": "0912345678" } ``` --- ## 3. Sản phẩm (Products) & Danh mục (Categories) ### 3.1 Lấy danh sách sản phẩm - **Endpoint:** `GET /api/products` - **Query string (Tùy chọn):** `?search=keyword` để tìm kiếm sản phẩm theo tên. ### 3.2 Xem chi tiết một sản phẩm - **Endpoint:** `GET /api/products/:id` ### 3.3 Thêm / Sửa / Xóa Sản phẩm (Dành cho Admin) - **Thêm sản phẩm:** `POST /api/products` - **Cập nhật:** `PUT /api/products/:id` - **Xóa:** `DELETE /api/products/:id` - **Body khi Thêm/Sửa (JSON):** ```json { "name": "Tên sản phẩm", "price": 100000, "description": "Mô tả", "imageUrl": "http://...", "category_id": 1 } ``` ### 3.4 Danh mục (Categories) - **Lấy danh sách:** `GET /api/categories` - **Thêm mới:** `POST /api/categories` *(Body: `{"name": "Tên DM"}`)* - **Cập nhật:** `PUT /api/categories/:id` - **Xóa:** `DELETE /api/categories/:id` --- ## 4. Giỏ hàng & Đơn hàng (Cart & Orders) *Yêu cầu Header: `Authorization: Bearer <token>`* ### 4.1 Lấy giỏ hàng - **Endpoint:** `GET /api/cart` ### 4.2 Thêm vào giỏ hàng - **Endpoint:** `POST /api/cart` - **Body (JSON):** ```json { "id": 1, "name": "Sản phẩm A", "price": 100000, "quantity": 2 } ``` ### 4.3 Đặt hàng (Checkout) - **Endpoint:** `POST /api/checkout` - **Body (JSON):** ```json { "total_amount": 200000, "shipping_address": "123 Le Loi, TP.HCM" } ``` ### 4.4 Lấy lịch sử đơn hàng cá nhân - **Endpoint:** `GET /api/orders/my-orders` ### 4.5 Lấy chi tiết một đơn hàng - **Endpoint:** `GET /api/orders/:id` ### 4.6 Hủy đơn hàng - **Endpoint:** `PUT /api/orders/:id/cancel` - **Mô tả:** Chuyển trạng thái đơn hàng sang `canceled`. Chỉ được thực hiện khi đơn hàng chưa giao. --- ## 5. Mã Giảm Giá (Coupons) ### 5.1 Áp dụng mã giảm giá - **Endpoint:** `POST /api/apply-coupon` - **Mô tả:** Tính toán tổng tiền sau khi giảm. Trả về cấu trúc JSON chứa `discount_amount` và `final_amount`. - **Body (JSON):** ```json { "code": "SAVE10", "total_amount": 500000, "user_id": 1 } ``` ### 5.2 Lấy danh sách mã giảm giá (Dành cho Admin) - **Endpoint:** `GET /api/coupons` - **Header:** `Authorization: Bearer <token>` --- ## 6. API Dành cho Admin *Tất cả API dưới đây yêu cầu `Authorization: Bearer <token>` và tài khoản phải có quyền Admin.* ### 6.1 Quản lý Người dùng - **Lấy danh sách người dùng:** `GET /api/admin/users` - **Xóa người dùng:** `DELETE /api/admin/users/:id` ### 6.2 Quản lý Đơn hàng (Toàn hệ thống) - **Lấy danh sách đơn hàng:** `GET /api/admin/orders` - **Cập nhật trạng thái đơn hàng:** `PUT /api/admin/orders/:id/status` - **Body (JSON):** `{"status": "confirmed"}` (Các trạng thái: `pending`, `confirmed`, `shipping`, `delivered`, `canceled`). ### 6.3 Import Sản phẩm từ CSV (JSON Array) - **Endpoint:** `POST /api/admin/import-products` - **Body (JSON):** ```json { "products": [ { "name": "SP 1", "price": 10000, "description": "Mô tả 1", "imageUrl": "", "category_id": 1 } ] } ``` ### 6.4 Quản lý Mã Giảm Giá - **Thêm mới mã:** `POST /api/admin/coupons` - **Body (JSON):** ```json { "code": "TET2025", "type": "percent", "discount_value": 15, "min_order_amount": 200000, "expired_at": "2025-01-31", "max_uses_per_user": 1 } ``` - **Xóa mã:** `DELETE /api/admin/coupons/:id` Look at the above API specification. Please read it and generate a Postman Collection in JSON format following this End-to-End flow: Admin Login -> Admin creates a new coupon -> Admin views the list of coupons -> User Login -> Apply that new coupon (User) -> Admin verify that the used coupon disappear or not -> Admin create another new coupon -> Admin delete that new coupon -> Admin verify that used coupon disappear or not . Write test scripts for this collection to verify each step. Use collection variables to store and pass the authentication token between requests; do not use environment or global variables. If you are at the login step, the admin's email is admin@eshop.com and the password is Admin123!

## AI Output:
<blockquote>
The collection EShop Admin Coupon Flow has been created with all 3 requests in the correct order, utilizing collection variables for authentication. Here's a summary of what was built:

### Collection Variables:

| Variable           | Value                 | Set by     |
|--------------------|-----------------------|------------|
| baseUrl            | http://localhost:3000 | Pre-set    |
| adminToken         | (auto-filled)         | Request 1  |
| couponId           | (auto-filled)         | Request 2  |
| couponCode         | (auto-filled)         | Request 2  |
| userToken          | (auto-filled)         | Request 4  |
| userId             | (auto-filled)         | Request 4  |
| couponToDeleteId   | (auto-filled)         | Request 7  |
| couponToDeleteCode | (auto-filled)         | Request 7  |

---
### Request flow:
| # | Request | Method | Endpoint | Key Action |
|---|---------|--------|----------|------------|
| 1 | Admin Login | POST | `/api/login` | Authenticates Admin, saves `adminToken` |
| 2 | Admin Create Coupon | POST | `/api/admin/coupons` | Creates `SAVE10`, saves `couponId` & `couponCode` |
| 3 | Admin View Coupon List | GET | `/api/admin/coupons` | Asserts `SAVE10` exists in list |
| 4 | User Login | POST | `/api/login` | Authenticates User, saves `userToken` & `userId` |
| 5 | User Apply Coupon | POST | `/api/apply-coupon` | Applies `{{couponCode}}` with `{{userId}}`, checks `discount_amount` & `final_amount` returned |
| 6 | Admin Verify Used Coupon Status | GET | `/api/admin/coupons` | Checks `SAVE10` is removed or marked used/inactive |
| 7 | Admin Create Another Coupon | POST | `/api/admin/coupons` | Creates `DELETE20`, saves `couponToDeleteId` & `couponToDeleteCode` |
| 8 | Admin Delete That Coupon | DELETE | `/api/admin/coupons/{{couponToDeleteId}}` | Deletes `DELETE20`, expects status `200` or `204` |
| 9 | Admin Verify Deleted Coupon Disappears | GET | `/api/admin/coupons` | Asserts `DELETE20` is no longer in the list |
</blockquote>

## AI's Postman collection:

[E2E - Coupon Create and Use Flow.postman_collection.json](./ai-postman-collections/E2E%20-%20Coupon%20Flow.postman_collection.json)

## Verdict:

INCOMPLETE

## Reasoning:
- It just generates an example account (test@domain.com) in the User Login request, which might be invalid or not properly seeded in the actual testing environment, leading to a 401 Unauthorized error and failing the subsequent flow.
- Need to change the first coupon that created in AI's response because the value code is same as the exist one in database
- The request bodies for both Admin Create Coupon and Admin Create Another Coupon are formatted incorrectly. The testing code sends "discount" and "expiryDate", but the API specification strictly requires "type", "discount_value", "min_order_amount", "expired_at", and "max_uses_per_user". The server might reject this payload with a 400 Bad Request.
- It make mistake in API get list of coupon is {{baseUrl}}/api/admin/coupons with is wrong in API specification
- API specification does not have api to get data from table coupon_usage so AI hallucinates it call the api get coupon to check
- We can use API admin before login as admin successfully so AI make big mistake
- We still need to enter couponCode and couponID for stage User apply coupon and Admin delete coupon

## Student fix:

- I need to change the login credentials to another valid value (test@eshop.com - Test1234!) so we can successfully log in as a user.
- I need to change the first coupon code that created in AI's response to "SAVETEST"
- I need to update the JSON payload in both coupon creation requests to match the exact schema defined in the API specification. For example, the body should be modified from {"code": "SAVE10", "discount": 10, "expiryDate": "2027-12-31"} to a format like {"code": "SAVE10", "type": "percent", "discount_value": 10, "min_order_amount": 0, "expired_at": "2027-12-31", "max_uses_per_user": 1}.
- I change the API get list of coupon to {{baseUrl}}/api/coupons and its path
- Erase step Admin Verify Used Coupon Status in pipeline
- I add condition at the user login stage for avoiding using admin api before login
- I use "pm.collectionVariables.set" to save and enter the input throughout whole stage