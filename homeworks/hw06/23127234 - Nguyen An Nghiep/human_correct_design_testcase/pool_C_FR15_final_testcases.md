# Pool C - FR-15 Product Management - Final Corrected Testcases

Base URL: [http://localhost:3000](http://localhost:3000)  
Primary API: `POST /api/products`  

## Header profiles used by this pool

The repeated Headers column is removed from the table. Apply the profile that matches the testcase objective:

- `X-Student-Id: 23127234; Authorization: Bearer {{adminToken}}; Content-Type: application/json`
- `X-Student-Id: 23127234; Authorization: Bearer {{adminToken}}; Content-Type: text/plain`
- `X-Student-Id: 23127234`
- `X-Student-Id: 23127234; Authorization: Bearer {{userToken}}; Content-Type: application/json`

## Final testcase table

| ID | Technique / Tag | Objective | Preconditions | Request (Method + URL) | Input | Steps | Expected Status | Expected Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-C-01 | Schema validation — SCHEMA-FR15: Positive/full body | Create a product with all documented fields and verify schema/read-back/cleanup. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "HW06 Full {{uniqueSuffix}}", "price":100000, "description": "Full product", "imageUrl": "https://example.test/p.png", "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 | Success JSON with generated positive id; GET matches intended fields; unrelated product unchanged; cleanup succeeds. |
| TC-C-02 | Domain partition — Positive/minimum body | Create using only required name, price, and existing category. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "HW06 Min {{uniqueSuffix}}", "price":1, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 | Create succeeds under A-007; optionals absent/null on read-back; cleanup succeeds. |
| TC-C-03 | Domain partition — BVA/name minimum | Create with a one-character name. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "A", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 | Accepted; stored name is A; cleanup succeeds. |
| TC-C-04 | Domain partition — BVA/name maximum | Create with exactly 255 characters in name. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "{{name255}}", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 | Accepted with exact 255-character stored name; cleanup succeeds. |
| TC-C-05 | Domain partition — Required/missing name | Reject missing name. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | JSON validation error; no row inserted. |
| TC-C-06 | Domain partition — Required/null name | Reject null name. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name":null, "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-07 | Domain partition — Required/empty name | Reject empty-string name. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-08 | Domain partition — Whitespace partition | Reject whitespace-only name after trimming. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "   ", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no blank product row. |
| TC-C-09 | Domain partition — BVA/name above maximum | Reject a 256-character name. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "{{name256}}", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-10 | Domain partition — Unicode name | Create a valid Vietnamese/Unicode product name. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Điện thoại thử nghiệm {{uniqueSuffix}}", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 | Accepted without encoding corruption; exact name read back; cleanup succeeds. |
| TC-C-11 | Domain partition — Required/missing price | Reject missing price. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "No Price {{uniqueSuffix}}", "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-12 | Domain partition — Required/null price | Reject null price. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Null Price {{uniqueSuffix}}", "price":null, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-13 | Domain partition — BVA/zero price | Reject price zero. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Zero Price {{uniqueSuffix}}", "price":0, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-14 | Domain partition — Negative price | Reject negative price. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Negative Price {{uniqueSuffix}}", "price":-1, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-15 | Domain partition — Positive price | Accept a positive integer price. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Positive Price {{uniqueSuffix}}", "price":1, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 | Created; read-back price is numeric 1; cleanup succeeds. |
| TC-C-16 | Domain partition — Decimal price | Verify a positive decimal price according to the documented number &gt; 0 rule. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Decimal Price {{uniqueSuffix}}", "price":1.5, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 under the literal requirement; mark result for A-008 confirmation | Created product preserves a positive numeric price without truncation; cleanup succeeds. If policy rejects decimals, update the requirement before changing the oracle. |
| TC-C-17 | Domain partition — Wrong type/numeric string | Reject numeric-string price. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "String Price {{uniqueSuffix}}", "price": "1000", "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no implicit string coercion/insertion. |
| TC-C-18 | Domain partition — Wrong type/boolean | Reject Boolean price. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Boolean Price {{uniqueSuffix}}", "price":true, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-19 | Domain partition — Large numeric boundary | Create with a very large positive safe integer price. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Large Price {{uniqueSuffix}}", "price":9007199254740991, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 or documented 400 | No unhandled 5xx/overflow or silent precision corruption; if accepted, exact numeric round-trip; if rejected, documented validation and no row. |
| TC-C-20 | Domain partition — Required/missing category | Reject missing category_id. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "No Category {{uniqueSuffix}}", "price":1000} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-21 | Domain partition — Required/null category | Reject null category_id. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Null Category {{uniqueSuffix}}", "price":1000, "category_id":null} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-22 | Domain partition — Referential integrity | Reject nonexistent positive category. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Bad Category {{uniqueSuffix}}", "price":1000, "category_id":999999999} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no orphan product inserted. |
| TC-C-23 | Domain partition — Category boundary | Reject category ID zero. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Zero Category {{uniqueSuffix}}", "price":1000, "category_id":0} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-24 | Domain partition — Category boundary | Reject negative category ID. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Negative Category {{uniqueSuffix}}", "price":1000, "category_id":-1} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no row inserted. |
| TC-C-25 | Domain partition — Category wrong type | Reject string category ID. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "String Category {{uniqueSuffix}}", "price":1000, "category_id": "1"} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 | Validation error; no implicit coercion/insertion. |
| TC-C-26 | Domain partition — Optional fields/empty strings | Create with empty description and imageUrl strings. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Optional Empty {{uniqueSuffix}}", "price":1000, "description": "", "imageUrl": "", "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 | Accepted under A-007; read-back preserves or consistently normalizes empty optional strings; cleanup succeeds. |
| TC-C-27 | Domain partition — Optional fields/null | Create with explicit null description and imageUrl. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Optional Null {{uniqueSuffix}}", "price":1000, "description":null, "imageUrl":null, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 | Accepted under A-007; null values read back consistently; cleanup succeeds. |
| TC-C-28 | Domain partition — URL validation | Verify a non-URL string is handled according to the documented optional-string contract. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "Bad URL {{uniqueSuffix}}", "price":1000, "imageUrl": "not-a-url", "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 under A-007 | Created product stores the string as data without server-side fetch/execution; cleanup succeeds. If URL validation is desired, add a requirement first. |
| TC-C-29 | Domain partition — Uniqueness | Observe duplicate-name behavior without treating duplicates as a defect. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | Create two valid products with identical unique name. | 1. Generate one unique valid product body. 2. POST it and capture the first ID if accepted. 3. POST the same name again with otherwise valid data and capture the second response/ID. 4. Record duplicate policy; if both succeed, verify distinct IDs and isolated records. 5. Delete every created fixture and verify both are absent. | Record 2xx or documented 4xx; never 5xx | Document duplicate policy. If accepted, each generated ID is distinct and both fixtures are cleaned; no uniqueness defect is filed. |
| TC-C-30 | Domain partition — Malformed JSON | Reject malformed JSON body. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name":"Broken","price":1000, | 1. Record baseline. 2. Send deliberately malformed raw JSON with admin auth. 3. Assert client error/non-leaking response. 4. Verify no product row was inserted. | 400 | Client JSON parse error without stack/path leakage; no row inserted. |
| TC-C-31 | Domain partition — Media type | Verify wrong media type is safely rejected without insertion. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | Valid product JSON bytes with Content-Type: text/plain | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 or 415 | Client error; no product inserted; no stack/parser internals leaked. |
| TC-C-32 | Security — Property-level authorization | Probe unexpected id/role properties for broken object-property authorization. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"id":1, "role": "admin", "name": "Mass Assign {{uniqueSuffix}}", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 400 or 200 with safe ignore under A-019 | If rejected, no row. If accepted, server generates its own ID, role is ignored/not stored, intended product fields remain correct, and cleanup succeeds. |
| TC-C-33 | Security — SEC-02: Authentication/missing | Attempt product creation without Authorization. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "No Auth {{uniqueSuffix}}", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 401 | Unauthorized JSON; no row inserted. |
| TC-C-34 | Security — SEC-03: Function-level authorization | Attempt product creation with normal user JWT. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name": "User Role {{uniqueSuffix}}", "price":1000, "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 403 | Forbidden JSON; no row inserted. |
| TC-C-35 | Security — SEC-04: Injection/XSS | Verify SQL-like name and HTML-like description are treated only as data and cannot alter queries or create executable API output. | Backend is running; admin/user tokens and an existing category fixture are available; uniqueSuffix is generated; baseline unrelated product is recorded. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name":"' OR 1=1 -- {{uniqueSuffix}}", "price":1000, "description": "&lt;script&gt;alert(1)&lt;/script&gt;", "category_id":{{categoryId}}} | 1. Record baseline product and unique test name. 2. Send POST with the stated body/authentication. 3. Assert status and success/error schema. 4. If accepted, capture ID and GET the product; compare stored values and unrelated product. 5. If rejected, verify no row with the unique name exists. 6. Delete any created fixture and verify cleanup. | 200 or documented 400; never 5xx | No SQL broadening/error leakage or HTML execution claim. If accepted, exact strings are stored as data, unrelated products unchanged, and fixture cleaned; if rejected, no row. Recommend splitting during human extension. |

## Student-added extension test case

| ID | Technique / Tag | Objective | Preconditions | Request (Method + URL) | Input | Steps | Expected Status | Expected Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-C-36 | Security — SEC-03: Role escalation/prototype pollution | Verify a normal user cannot bypass the admin-role check by injecting `role` or `__proto__` properties into an otherwise valid product body. | A valid normal-user JWT, valid admin JWT, existing category, unique suffix, and baseline product list are available. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | {"name":"Role Bypass {{uniqueSuffix}}","price":1000,"category_id":{{categoryId}},"role":"admin","__proto__":{"role":"admin"}} | 1. Record the product baseline and unique test name. 2. Send the body with `{{userToken}}`. 3. Assert a generic forbidden JSON response. 4. Verify no product with the unique name exists. 5. Repeat a normal-user create with an ordinary valid body and verify it is still forbidden. 6. Perform one ordinary admin create/read/delete control and verify cleanup and unrelated data integrity. | 403 | Both normal-user requests are forbidden; no role escalation, prototype pollution, or product insertion occurs. The admin control still works normally, its fixture is removed, and unrelated products remain unchanged. |
