# Pool A - FR-05 Product Listing and Search - AI-Generated Testcases

Base URL: [http://localhost:3000](http://localhost:3000)  
Primary API: `GET /api/products`  
Classification summary: **22 VALID / 5 INVALID / 8 INCOMPLETE**.

## Header profiles used by this pool

The repeated Headers column is removed from the table. Apply the profile that matches the testcase objective:

- `X-Student-Id: 23127234`

## AI-generated testcase table

| ID | Technique / Tag | Objective | Preconditions | Request (Method + URL) | Input | Steps | Expected Status | Expected Result | Classification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-A-01 | Domain partition — Positive/list | List all products when search is omitted. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products | &lt;omitted&gt; | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | JSON array; each item follows the approved product schema; no sensitive fields; GET causes no mutation. | VALID |
| TC-A-02 | Domain partition — Equivalence/exact | Search by an exact full product name. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | iPhone 15 Pro Max | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Array contains the exact named product and no product whose name does not contain the keyword. | VALID |
| TC-A-03 | Domain partition — Equivalence/prefix | Search with a product-name prefix. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | iPhone | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Every returned product name contains the prefix under the observed matching policy; expected seeded iPhone is included. | VALID |
| TC-A-04 | Domain partition — Equivalence/middle | Search with a middle token from a product name. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | Galaxy | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Every result name contains Galaxy; Samsung Galaxy seed product is included. | VALID |
| TC-A-05 | Domain partition — Equivalence/suffix | Search with a suffix token. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | Ultra | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Every returned name contains Ultra; no unrelated product is returned. | VALID |
| TC-A-06 | Domain partition — Case sensitivity | Require lowercase search to match mixed-case iPhone. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | iphone | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Result must equal the exact-case result because search is case-insensitive. | INCOMPLETE |
| TC-A-07 | Domain partition — Case sensitivity | Require uppercase search to return the same set as exact case. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | IPHONE | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Result set must exactly equal the exact-case result. | INCOMPLETE |
| TC-A-08 | Domain partition — Unicode/diacritics | Search using exact Vietnamese diacritics. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | Điện thoại | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Valid JSON array; any result must contain the exact Vietnamese keyword under the observed matching policy; no encoding corruption. | VALID |
| TC-A-09 | Domain partition — Unicode normalization | Require NFC and NFD inputs to return identical results. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | NFC/NFD pair for Điện thoại | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Both Unicode forms must return identical result sets. | INCOMPLETE |
| TC-A-10 | Domain partition — Boundary/minimal text | Search with a one-character keyword. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | a | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | JSON array; each result name contains the keyword under the observed policy; no unrelated schema fields or mutation. | VALID |
| TC-A-11 | Security — Optional parameter | Verify omitted search invokes list behavior. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products | &lt;omitted&gt; | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Non-error JSON product list with approved minimal schema. | INVALID |
| TC-A-12 | Domain partition — Empty input | Require empty search to equal omitted search. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | — | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Exact same ordered array as omitted search. | INCOMPLETE |
| TC-A-13 | Domain partition — Whitespace partition | Require whitespace to be trimmed and return all products. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} |     | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Response must exactly equal omitted search. | INVALID |
| TC-A-14 | Domain partition — Negative/no match | Search for a guaranteed non-matching keyword. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | __NO_PRODUCT_23127234__ | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Empty JSON array; response is not an error and no data changes. | VALID |
| TC-A-15 | Domain partition — Numeric text | Search using numeric text as a name keyword. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | 123456789 | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | JSON array, normally empty unless a name contains the digits; no conversion error or mutation. | VALID |
| TC-A-16 | Domain partition — Punctuation | Search using punctuation/hyphen text. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | Pro-Max | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Safe JSON response; any result is consistent with literal/observed name matching; no internal leakage. | VALID |
| TC-A-17 | Domain partition — URL encoding | Verify encoded plus is parsed safely. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | A%2BB | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | JSON response; decoded plus does not corrupt query parsing, execute code, or leak internals. | VALID |
| TC-A-18 | Domain partition — URL encoding | Verify encoded ampersand remains within the search value. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | A%26B | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | JSON response; encoded ampersand does not create an unintended parameter or internal error. | VALID |
| TC-A-19 | Security — SEC-05: Wildcard semantics | Require percent to be treated literally and return no products. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | % | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Empty array because percent is not a wildcard. | INCOMPLETE |
| TC-A-20 | Security — SEC-05: Wildcard semantics | Require underscore to be treated literally. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | _ | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Only product names containing a literal underscore may be returned. | INCOMPLETE |
| TC-A-21 | Domain partition — Unicode robustness | Search using an emoji keyword. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | 📱 | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Valid UTF-8 JSON, no encoding/internal error, and no mutation; normally empty unless a name contains the emoji. | VALID |
| TC-A-22 | Domain partition — Boundary/long text | Search with a 255-character keyword. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | 255-character safe string | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Safe JSON response, normally empty; no truncation-related 5xx, internal leakage, or mutation. | VALID |
| TC-A-23 | Domain partition — Extreme length/performance | Require a 10,000-character search to complete under two seconds. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | 10,000-character string | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | &lt; 2000 ms and 200 | Response must complete under the lecture's two-second example threshold. | INVALID |
| TC-A-24 | Domain partition — HTTP parameter pollution | Require the last duplicate search parameter to win. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search=iPhone&amp;search=Galaxy | search=iPhone&amp;search=Galaxy | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Only Galaxy results are returned. | INVALID |
| TC-A-25 | Domain partition — Unknown parameter | Require unknown query parameters to be ignored and equal baseline. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?unknown=value | ?unknown=value | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Exact same ordered response as baseline. | INCOMPLETE |
| TC-A-26 | Security — SEC-05: SQL injection/quote | Send a single quote and verify it is handled as data. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | ' | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 or safe client 4xx | No SQL syntax/internal error, HTML diagnostic, mutation, or secret leakage; baseline smoke call remains available. | VALID |
| TC-A-27 | Security — SEC-05: SQL injection/boolean | Attempt boolean-based SQL injection through search. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | ' OR 1=1 -- | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 or safe client 4xx | Injection must not broaden results by executing SQL, expose internals, or mutate data; payload is treated as text. | VALID |
| TC-A-28 | Security — SEC-05: SQL injection/UNION | Attempt UNION-style SQL injection. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | ' UNION SELECT 1,2,3,4,5,6 -- | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 or safe client 4xx | No injected columns/rows, database error details, HTML diagnostics, or mutation. | VALID |
| TC-A-29 | Security — SEC-05: SQL injection/destructive | Attempt destructive-looking stacked SQL input safely on the local fixture database. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | '; DROP TABLE products; -- | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 or safe client 4xx | Products remain queryable and unchanged; no command executes; no internal leakage. | VALID |
| TC-A-30 | Security — SEC-04: XSS/error handling | Submit an XSS payload as the search keyword. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | &lt;script&gt;alert(1)&lt;/script&gt; | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 or safe client 4xx | Response remains JSON/plain data, contains no executable HTML page, and leaks no internals; API/UI execution is not claimed. | VALID |
| TC-A-31 | Security — Control character | Submit encoded NUL/newline/control characters. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products?search={{searchValue}} | %00%0A%0D | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 or safe client 4xx | No crash, header splitting, HTML/internal leakage, or mutation; response follows declared content type. | VALID |
| TC-A-32 | Domain partition — Unsupported method | Require unsupported POST to list endpoint to return 405. | Backend is running with known seed products; baseline product list is available. | POST<br>[http://localhost:3000](http://localhost:3000)/api/products | POST /api/products without body | 1. Record baseline. 2. Send POST with no product body and only student header. 3. Assert a client error. 4. Verify no product was created. | 405 | Method Not Allowed JSON error. | INVALID |
| TC-A-33 | Schema validation — SCHEMA-01: Header/contract | Verify successful product list declares JSON content type. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products | &lt;omitted&gt; | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Content-Type contains application/json and body parses as JSON array. | VALID |
| TC-A-34 | Schema validation — SCHEMA-01: Strict schema | Reject any product response with an extra field. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products | &lt;omitted&gt; | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | Every item has exactly id,name,price,description,imageUrl,category_id and additionalProperties=false. | INCOMPLETE |
| TC-A-35 | Schema validation — SCHEMA-01: Data integrity | Validate product-list data integrity and confidentiality. | Backend is running with known seed products; baseline product list is available. | GET<br>[http://localhost:3000](http://localhost:3000)/api/products | &lt;omitted&gt; | 1. Record a baseline list/hash. 2. Set searchValue to the stated input. 3. Send GET with the student header. 4. Assert status, JSON/content-type, values/schema, and no internal leakage. 5. For hostile/extreme input, repeat the baseline smoke call and confirm data remains available. | 200 | IDs are positive and unique within response; names non-empty; prices positive numbers; categories positive IDs; no password/token/reset/role fields. | VALID |
