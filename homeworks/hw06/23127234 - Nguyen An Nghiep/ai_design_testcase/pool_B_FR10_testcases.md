# Pool B - FR-10 Order State Machine - AI-Generated Testcases

Base URL: [http://localhost:3000](http://localhost:3000)  
Primary API: `PUT /api/orders/:id/cancel`  
Classification summary: **28 VALID / 3 INVALID / 4 INCOMPLETE**.

## Header profiles used by this pool

The repeated Headers column is removed from the table. Apply the profile that matches the testcase objective:

- `X-Student-Id: 23127234; Authorization: Bearer {{userToken}}`
- `X-Student-Id: 23127234`
- `X-Student-Id: 23127234; Authorization: Bearer `
- `X-Student-Id: 23127234; Authorization: Basic dGVzdDp0ZXN0`
- `X-Student-Id: 23127234; Authorization: Bearer {{tamperedUserToken}}`
- `X-Student-Id: 23127234; Authorization: Bearer {{userToken}}; Content-Type: application/json`
- `X-Student-Id: 23127234; Authorization: Bearer {{adminToken}}; Content-Type: application/json`

## AI-generated testcase table

| ID | Technique / Tag | Objective | Preconditions | Request (Method + URL) | Input | Steps | Expected Status | Expected Result | Classification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-B-01 | State transition — Valid transition: Cancel an own pending order | Cancel an own pending order. | Independent order owned by userToken is pending. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdPendingCancel}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | JSON success; read-back status is canceled; owner and unrelated order unchanged. | INCOMPLETE |
| TC-B-02 | State transition — Valid transition: Cancel an own confirmed order | Cancel an own confirmed order. | Independent owner order is confirmed by admin. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdConfirmedCancel}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | JSON success; stored status becomes canceled. | VALID |
| TC-B-03 | State transition — Invalid transition: Reject owner cancellation of a shipping order | Reject owner cancellation of a shipping order. | Independent owner order is shipping. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdShippingReject}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | JSON error; stored status remains shipping. | VALID |
| TC-B-04 | State transition — Final state: Reject cancellation of a delivered order | Reject cancellation of a delivered order. | Independent owner order is delivered. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdDeliveredReject}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | JSON error; delivered remains unchanged. | VALID |
| TC-B-05 | State transition — Final state/retry: Reject a second cancellation of an already canceled order | Reject a second cancellation of an already canceled order. | Independent owner order is canceled. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdCanceledReject}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | JSON error; state remains canceled and no duplicate side effect. | VALID |
| TC-B-06 | Domain partition — Path/nonexistent | Cancel a nonexistent positive order ID. | Use a verified nonexistent large ID. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/999999999/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 404 | Non-leaking JSON not-found error; no order changes. | VALID |
| TC-B-07 | Security — BOLA/IDOR | Attempt to cancel user B's pending order with user A token. | orderIdOtherUser belongs to user2Token and is pending. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdOtherUser}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 403 | Forbidden and user B's order remains pending. | INCOMPLETE |
| TC-B-08 | Domain partition — Path boundary | Cancel order ID zero. | No valid order has ID 0. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/0/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 or 404 | Client error without SQL/internal leakage; no state mutation. | VALID |
| TC-B-09 | Domain partition — Path boundary | Cancel negative order ID. | No valid order has a negative ID. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/-1/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 or 404 | Client error; no leakage or mutation. | VALID |
| TC-B-10 | Domain partition — Path wrong type | Cancel nonnumeric order ID. | Backend and baseline orders available. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/abc/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 or 404 | Client error, no SQL/internal leakage, no mutation. | VALID |
| TC-B-11 | Domain partition — Path extreme | Cancel an extremely large numeric ID. | Use value greater than normal database range. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/999999999999999999999/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 or 404 | Safe client error; no precision-related cross-order mutation or leakage. | VALID |
| TC-B-12 | Security — SEC-02: Authentication/missing | Call cancel without Authorization. | Own pending order ID exists. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdPendingAuth}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 401 | Unauthorized JSON; order remains pending. | VALID |
| TC-B-13 | Security — SEC-02: Authentication/empty | Call cancel with empty Bearer token. | Own pending order exists. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdPendingAuth}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 401 or 403 | Authentication error; order unchanged; no token parsing details. | VALID |
| TC-B-14 | Security — SEC-02: Authentication/scheme | Call cancel with Basic scheme instead of Bearer. | Own pending order exists. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdPendingAuth}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 401 | Unauthorized; no state change. | VALID |
| TC-B-15 | Security — SEC-02: Authentication/tamper | Call cancel with a tampered JWT signature. | Own pending order exists; token copy has one signature character changed. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdPendingAuth}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 403 | Forbidden/authentication error; order remains pending; no secret leakage. | VALID |
| TC-B-16 | Security — SEC-03: Function-level authorization | Use normal user token on admin status endpoint. | Independent pending order exists. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdRoleCheck}}/status | {"status":"confirmed"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 403 | Role error; stored state remains pending. | VALID |
| TC-B-17 | State transition — Valid state edge: Admin transitions pending to confirmed | Admin transitions pending to confirmed. | Independent order is pending; admin token valid. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdFullForward}}/status | {"status":"confirmed"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | Success JSON; stored state confirmed. | VALID |
| TC-B-18 | State transition — Valid state edge: Admin transitions confirmed to shipping | Admin transitions confirmed to shipping. | Same forward-path order is confirmed after AI-FR10-017. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdFullForward}}/status | {"status":"shipping"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | Success JSON; stored state shipping. | VALID |
| TC-B-19 | State transition — Valid state edge: Admin transitions shipping to delivered | Admin transitions shipping to delivered. | Same forward-path order is shipping after AI-FR10-018. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdFullForward}}/status | {"status":"delivered"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | Success JSON; stored state delivered and final. | VALID |
| TC-B-20 | State transition — Valid cancel edge/admin: Admin transitions pending to canceled | Admin transitions pending to canceled. | Independent order is pending. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdPendingAdminCancel}}/status | {"status":"canceled"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | Success; stored state canceled and final. | VALID |
| TC-B-21 | State transition — Valid cancel edge/admin: Admin transitions confirmed to canceled | Admin transitions confirmed to canceled. | Independent order is confirmed. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdConfirmedAdminCancel}}/status | {"status":"canceled"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | Success; stored state canceled and final. | VALID |
| TC-B-22 | State transition — Invalid skipped edge: Reject pending to shipping | Reject pending to shipping. | Independent order is pending. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdPendingInvalid1}}/status | {"status":"shipping"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Error; state remains pending. | VALID |
| TC-B-23 | State transition — Invalid skipped edge: Reject pending to delivered | Reject pending to delivered. | Independent order is pending. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdPendingInvalid2}}/status | {"status":"delivered"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Error; state remains pending. | VALID |
| TC-B-24 | State transition — Invalid skipped edge: Reject confirmed to delivered | Reject confirmed to delivered. | Independent order is confirmed. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdConfirmedInvalid}}/status | {"status":"delivered"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Error; state remains confirmed. | VALID |
| TC-B-25 | State transition — Invalid backward edge: Reject shipping to pending | Reject shipping to pending. | Independent order is shipping. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdShippingInvalid1}}/status | {"status":"pending"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Error; state remains shipping. | VALID |
| TC-B-26 | State transition — Invalid backward edge: Reject shipping to confirmed | Reject shipping to confirmed. | Independent order is shipping. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdShippingInvalid2}}/status | {"status":"confirmed"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Error; state remains shipping. | VALID |
| TC-B-27 | State transition — Ambiguous edge: Allow admin to cancel a shipping order | Allow admin to cancel a shipping order. | Independent order is shipping. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdShippingAdminCancel}}/status | {"status":"canceled"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | Admin override succeeds and state becomes canceled. | INVALID |
| TC-B-28 | State transition — Final-state edge: Reject delivered to pending | Reject delivered to pending. | Independent order is delivered. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdDeliveredFinal}}/status | {"status":"pending"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Error; delivered remains unchanged. | VALID |
| TC-B-29 | State transition — Final-state edge: Allow canceled order to move to delivered | Allow canceled order to move to delivered. | Independent order is canceled. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdCanceledFinal}}/status | {"status":"delivered"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | State becomes delivered. | INVALID |
| TC-B-30 | State transition — Same-state/idempotency: Allow pending-to-pending as idempotent success | Allow pending-to-pending as idempotent success. | Independent order is pending. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdSameState}}/status | {"status":"pending"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | Success because the requested state already matches. | INVALID |
| TC-B-31 | Domain partition — Invalid enum | Reject unknown status value. | Independent pending order exists. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdInvalidStatus}}/status | {"status":"refunded"} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Invalid-transition/input JSON; state remains pending. | VALID |
| TC-B-32 | Domain partition — Missing field | Reject missing status property. | Independent pending order exists. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdMissingStatus}}/status | {} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Client error; state remains pending; no internal leakage. | VALID |
| TC-B-33 | Domain partition — Null field | Reject null status. | Independent pending order exists. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/admin/orders/{{orderIdNullStatus}}/status | {"status":null} | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 400 | Client error; state remains pending. | VALID |
| TC-B-34 | State transition — Concurrency/idempotency: Require two simultaneous cancels to both return 200 | Require two simultaneous cancels to both return 200. | One own pending order; two parallel user requests use the same ID. | PUT x2 concurrently<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdConcurrent}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 and 200 | Both requests succeed; final state canceled. | INCOMPLETE |
| TC-B-35 | Schema validation — SCHEMA-FR10: Schema/postcondition | Check cancel response contains a message. | Own pending order exists. | PUT<br>[http://localhost:3000](http://localhost:3000)/api/orders/{{orderIdSchema}}/cancel | No body | 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation. | 200 | Body has message property. | INCOMPLETE |
