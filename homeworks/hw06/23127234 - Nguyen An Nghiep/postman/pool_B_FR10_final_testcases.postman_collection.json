{
  "info": {
    "_postman_id": "11358121-1c8b-4c5c-a53e-79ad42aecf4c",
    "name": "HW06 23127234 - Pool B - FR-10 Order State Machine",
    "description": "Executable Postman collection generated from pool_B_FR10_final_testcases.md. Contains 39 testcase items and runtime-only authentication setup.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "const studentId = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
          "pm.request.headers.upsert({ key: 'X-Student-Id', value: String(studentId) });",
          "console.log(`[HW06][${pm.info.requestName}] X-Student-Id: ${studentId}`);"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "pm.test('Required X-Student-Id header is attached', function () {",
          "  const expected = String(pm.environment.get('studentId') || pm.collectionVariables.get('studentId'));",
          "  pm.expect(pm.request.headers.get('X-Student-Id')).to.eql(expected);",
          "});"
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3000",
      "type": "string"
    },
    {
      "key": "studentId",
      "value": "23127234",
      "type": "string"
    },
    {
      "key": "adminEmail",
      "value": "",
      "type": "string"
    },
    {
      "key": "adminPassword",
      "value": "",
      "type": "string"
    },
    {
      "key": "userEmail",
      "value": "",
      "type": "string"
    },
    {
      "key": "userPassword",
      "value": "",
      "type": "string"
    },
    {
      "key": "secondUserEmail",
      "value": "hw06-user-b-23127234@example.test",
      "type": "string"
    },
    {
      "key": "secondUserName",
      "value": "HW06 User B",
      "type": "string"
    },
    {
      "key": "jwtSecret",
      "value": "",
      "type": "string"
    },
    {
      "key": "adminToken",
      "value": "",
      "type": "string"
    },
    {
      "key": "userToken",
      "value": "",
      "type": "string"
    },
    {
      "key": "userBToken",
      "value": "",
      "type": "string"
    },
    {
      "key": "expiredUserToken",
      "value": "",
      "type": "string"
    },
    {
      "key": "tamperedUserToken",
      "value": "eyJhbGciOiJIUzI1NiJ9.eyJpZCI6Miwicm9sZSI6InVzZXIifQ.invalid-signature",
      "type": "string"
    },
    {
      "key": "currentOrderId",
      "value": "",
      "type": "string"
    },
    {
      "key": "fixtureError",
      "value": "",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "00 - Runtime setup",
      "item": [
        {
          "name": "Login User A",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/login",
            "description": "Runtime-only authentication setup. Credentials and returned JWTs are not stored in the exported collection.",
            "body": {
              "mode": "raw",
              "raw": "{\"email\":\"{{userEmail}}\",\"password\":\"{{userPassword}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Login User A returns 200', function () { pm.response.to.have.status(200); });",
                  "pm.test('Login User A returns a JWT', function () {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.token).to.be.a('string').and.not.empty;",
                  "  pm.collectionVariables.set('userToken', body.token);",
                  "});",
                  "pm.test('Generate an expired JWT fixture without storing it in the collection file', function () {",
                  "  const body = pm.response.json();",
                  "  const secret = pm.environment.get('jwtSecret') || pm.collectionVariables.get('jwtSecret');",
                  "  pm.expect(secret, 'Set jwtSecret as a local/environment value before the run').to.be.a('string').and.not.empty;",
                  "  function b64url(wordArray) { return CryptoJS.enc.Base64.stringify(wordArray).replace(/=+$/,'').replace(/\\+/g,'-').replace(/\\//g,'_'); }",
                  "  function text64(value) { return b64url(CryptoJS.enc.Utf8.parse(value)); }",
                  "  const header = text64(JSON.stringify({ alg: 'HS256', typ: 'JWT' }));",
                  "  const payload = text64(JSON.stringify({ id: body.user.id, role: body.user.role, exp: Math.floor(Date.now()/1000)-60 }));",
                  "  const unsigned = `${header}.${payload}`;",
                  "  const signature = b64url(CryptoJS.HmacSHA256(unsigned, secret));",
                  "  pm.collectionVariables.set('expiredUserToken', `${unsigned}.${signature}`);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Login Admin",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/login",
            "description": "Runtime-only authentication setup. Credentials and returned JWTs are not stored in the exported collection.",
            "body": {
              "mode": "raw",
              "raw": "{\"email\":\"{{adminEmail}}\",\"password\":\"{{adminPassword}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Login Admin returns 200', function () { pm.response.to.have.status(200); });",
                  "pm.test('Login Admin returns a JWT', function () {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.token).to.be.a('string').and.not.empty;",
                  "  pm.collectionVariables.set('adminToken', body.token);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Register User B",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/register",
            "description": "Creates a second owner for the IDOR testcase. Restarting the backend resets this local fixture database.",
            "body": {
              "mode": "raw",
              "raw": "{\"name\":\"{{secondUserName}}\",\"email\":\"{{secondUserEmail}}\",\"password\":\"{{userPassword}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Register User B returns success', function () { pm.expect([200, 201]).to.include(pm.response.code); });"
                ]
              }
            }
          ]
        },
        {
          "name": "Login User B",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/login",
            "description": "Runtime-only authentication setup. Credentials and returned JWTs are not stored in the exported collection.",
            "body": {
              "mode": "raw",
              "raw": "{\"email\":\"{{secondUserEmail}}\",\"password\":\"{{userPassword}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Login User B returns 200', function () { pm.response.to.have.status(200); });",
                  "pm.test('Login User B returns a JWT', function () {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.token).to.be.a('string').and.not.empty;",
                  "  pm.collectionVariables.set('userBToken', body.token);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "01 - Domain partitions",
      "item": [
        {
          "name": "TC-B-06 | Cancel a nonexistent positive order ID.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/999999999/cancel",
            "description": "Testcase: TC-B-06\n\nTechnique / Tag: Domain partition — Path/nonexistent\n\nObjective: Cancel a nonexistent positive order ID.\n\nPreconditions: Use a verified nonexistent large ID.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 404\n\nExpected result: Non-leaking JSON not-found error; no order changes."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.set('currentOrderId', '999999999');",
                  "pm.collectionVariables.unset('fixtureError');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [404];",
                  "pm.test('TC-B-06 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-06 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('TC-B-06 - invalid or missing ID returns a generic JSON error', function () { pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'); pm.expect(pm.response.text()).not.to.match(/user_id|shipping_address|total_amount/i); });"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-08 | Cancel order ID zero.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/0/cancel",
            "description": "Testcase: TC-B-08\n\nTechnique / Tag: Domain partition — Path boundary\n\nObjective: Cancel order ID zero.\n\nPreconditions: No valid order has ID 0.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400 or 404\n\nExpected result: Client error without SQL/internal leakage; no state mutation."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.set('currentOrderId', '0');",
                  "pm.collectionVariables.unset('fixtureError');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400,404];",
                  "pm.test('TC-B-08 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-08 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('TC-B-08 - invalid or missing ID returns a generic JSON error', function () { pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'); pm.expect(pm.response.text()).not.to.match(/user_id|shipping_address|total_amount/i); });"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-09 | Cancel negative order ID.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/-1/cancel",
            "description": "Testcase: TC-B-09\n\nTechnique / Tag: Domain partition — Path boundary\n\nObjective: Cancel negative order ID.\n\nPreconditions: No valid order has a negative ID.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400 or 404\n\nExpected result: Client error; no leakage or mutation."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.set('currentOrderId', '-1');",
                  "pm.collectionVariables.unset('fixtureError');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400,404];",
                  "pm.test('TC-B-09 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-09 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('TC-B-09 - invalid or missing ID returns a generic JSON error', function () { pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'); pm.expect(pm.response.text()).not.to.match(/user_id|shipping_address|total_amount/i); });"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-10 | Cancel nonnumeric order ID.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/abc/cancel",
            "description": "Testcase: TC-B-10\n\nTechnique / Tag: Domain partition — Path wrong type\n\nObjective: Cancel nonnumeric order ID.\n\nPreconditions: Backend and baseline orders available.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400 or 404\n\nExpected result: Client error, no SQL/internal leakage, no mutation."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.set('currentOrderId', 'abc');",
                  "pm.collectionVariables.unset('fixtureError');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400,404];",
                  "pm.test('TC-B-10 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-10 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('TC-B-10 - invalid or missing ID returns a generic JSON error', function () { pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'); pm.expect(pm.response.text()).not.to.match(/user_id|shipping_address|total_amount/i); });"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-11 | Cancel an extremely large numeric ID.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/999999999999999999999/cancel",
            "description": "Testcase: TC-B-11\n\nTechnique / Tag: Domain partition — Path extreme\n\nObjective: Cancel an extremely large numeric ID.\n\nPreconditions: Use value greater than normal database range.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400 or 404\n\nExpected result: Safe client error; no precision-related cross-order mutation or leakage."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.set('currentOrderId', '999999999999999999999');",
                  "pm.collectionVariables.unset('fixtureError');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400,404];",
                  "pm.test('TC-B-11 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-11 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('TC-B-11 - invalid or missing ID returns a generic JSON error', function () { pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'); pm.expect(pm.response.text()).not.to.match(/user_id|shipping_address|total_amount/i); });"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-31 | Reject unknown status value.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-31\n\nTechnique / Tag: Domain partition — Invalid enum\n\nObjective: Reject unknown status value.\n\nPreconditions: Independent pending order exists.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Invalid-transition/input JSON; state remains pending.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"refunded\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-31 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-31 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-31 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-32 | Reject missing status property.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-32\n\nTechnique / Tag: Domain partition — Missing field\n\nObjective: Reject missing status property.\n\nPreconditions: Independent pending order exists.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Client error; state remains pending; no internal leakage.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-32 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-32 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-32 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-33 | Reject null status.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-33\n\nTechnique / Tag: Domain partition — Null field\n\nObjective: Reject null status.\n\nPreconditions: Independent pending order exists.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Client error; state remains pending.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":null}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-33 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-33 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-33 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "02 - State transitions",
      "item": [
        {
          "name": "TC-B-01 | Cancel an own pending order.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-01\n\nTechnique / Tag: State transition — Valid transition: Cancel an own pending order\n\nObjective: Cancel an own pending order.\n\nPreconditions: Independent order owned by userToken is pending.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 200\n\nExpected result: JSON success; read-back status is canceled; owner and unrelated order unchanged."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200];",
                  "pm.test('TC-B-01 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-01 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-01 - persisted order state is canceled', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('canceled');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-02 | Cancel an own confirmed order.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-02\n\nTechnique / Tag: State transition — Valid transition: Cancel an own confirmed order\n\nObjective: Cancel an own confirmed order.\n\nPreconditions: Independent owner order is confirmed by admin.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 200\n\nExpected result: JSON success; stored status becomes canceled."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200];",
                  "pm.test('TC-B-02 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-02 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-02 - persisted order state is canceled', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('canceled');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-03 | Reject owner cancellation of a shipping order.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-03\n\nTechnique / Tag: State transition — Invalid transition: Reject owner cancellation of a shipping order\n\nObjective: Reject owner cancellation of a shipping order.\n\nPreconditions: Independent owner order is shipping.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: JSON error; stored status remains shipping."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\",\"shipping\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-03 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-03 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-03 - persisted order state is shipping', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('shipping');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-04 | Reject cancellation of a delivered order.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-04\n\nTechnique / Tag: State transition — Final state: Reject cancellation of a delivered order\n\nObjective: Reject cancellation of a delivered order.\n\nPreconditions: Independent owner order is delivered.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: JSON error; delivered remains unchanged."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\",\"shipping\",\"delivered\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-04 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-04 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-04 - persisted order state is delivered', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('delivered');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-05 | Reject a second cancellation of an already canceled order.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-05\n\nTechnique / Tag: State transition — Final state/retry: Reject a second cancellation of an already canceled order\n\nObjective: Reject a second cancellation of an already canceled order.\n\nPreconditions: Independent owner order is canceled.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: JSON error; state remains canceled and no duplicate side effect."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  pm.sendRequest({ url: `${base}/api/orders/${orderId}/cancel`, method: 'PUT', header: { 'X-Student-Id': String(sid), Authorization: `Bearer ${ownerToken}` } }, function (cancelErr, cancelRes) { if (cancelErr || cancelRes.code !== 200) fail(`Cancel fixture failed: ${cancelErr || cancelRes.code}`); });",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-05 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-05 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-05 - persisted order state is canceled', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('canceled');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-17 | Admin transitions pending to confirmed.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-17\n\nTechnique / Tag: State transition — Valid state edge: Admin transitions pending to confirmed\n\nObjective: Admin transitions pending to confirmed.\n\nPreconditions: Independent order is pending; admin token valid.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 200\n\nExpected result: Success JSON; stored state confirmed.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"confirmed\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200];",
                  "pm.test('TC-B-17 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-17 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-17 - persisted order state is confirmed', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('confirmed');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-18 | Admin transitions confirmed to shipping.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-18\n\nTechnique / Tag: State transition — Valid state edge: Admin transitions confirmed to shipping\n\nObjective: Admin transitions confirmed to shipping.\n\nPreconditions: Same forward-path order is confirmed after AI-FR10-017.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 200\n\nExpected result: Success JSON; stored state shipping.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"shipping\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200];",
                  "pm.test('TC-B-18 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-18 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-18 - persisted order state is shipping', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('shipping');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-19 | Admin transitions shipping to delivered.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-19\n\nTechnique / Tag: State transition — Valid state edge: Admin transitions shipping to delivered\n\nObjective: Admin transitions shipping to delivered.\n\nPreconditions: Same forward-path order is shipping after AI-FR10-018.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 200\n\nExpected result: Success JSON; stored state delivered and final.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"delivered\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\",\"shipping\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200];",
                  "pm.test('TC-B-19 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-19 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-19 - persisted order state is delivered', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('delivered');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-20 | Admin transitions pending to canceled.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-20\n\nTechnique / Tag: State transition — Valid cancel edge/admin: Admin transitions pending to canceled\n\nObjective: Admin transitions pending to canceled.\n\nPreconditions: Independent order is pending.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 200\n\nExpected result: Success; stored state canceled and final.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"canceled\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200];",
                  "pm.test('TC-B-20 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-20 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-20 - persisted order state is canceled', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('canceled');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-21 | Admin transitions confirmed to canceled.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-21\n\nTechnique / Tag: State transition — Valid cancel edge/admin: Admin transitions confirmed to canceled\n\nObjective: Admin transitions confirmed to canceled.\n\nPreconditions: Independent order is confirmed.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 200\n\nExpected result: Success; stored state canceled and final.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"canceled\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200];",
                  "pm.test('TC-B-21 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-21 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-21 - persisted order state is canceled', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('canceled');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-22 | Reject pending to shipping.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-22\n\nTechnique / Tag: State transition — Invalid skipped edge: Reject pending to shipping\n\nObjective: Reject pending to shipping.\n\nPreconditions: Independent order is pending.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Error; state remains pending.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"shipping\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-22 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-22 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-22 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-23 | Reject pending to delivered.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-23\n\nTechnique / Tag: State transition — Invalid skipped edge: Reject pending to delivered\n\nObjective: Reject pending to delivered.\n\nPreconditions: Independent order is pending.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Error; state remains pending.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"delivered\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-23 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-23 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-23 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-24 | Reject confirmed to delivered.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-24\n\nTechnique / Tag: State transition — Invalid skipped edge: Reject confirmed to delivered\n\nObjective: Reject confirmed to delivered.\n\nPreconditions: Independent order is confirmed.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Error; state remains confirmed.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"delivered\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-24 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-24 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-24 - persisted order state is confirmed', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('confirmed');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-25 | Reject shipping to pending.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-25\n\nTechnique / Tag: State transition — Invalid backward edge: Reject shipping to pending\n\nObjective: Reject shipping to pending.\n\nPreconditions: Independent order is shipping.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Error; state remains shipping.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"pending\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\",\"shipping\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-25 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-25 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-25 - persisted order state is shipping', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('shipping');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-26 | Reject shipping to confirmed.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-26\n\nTechnique / Tag: State transition — Invalid backward edge: Reject shipping to confirmed\n\nObjective: Reject shipping to confirmed.\n\nPreconditions: Independent order is shipping.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Error; state remains shipping.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"confirmed\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\",\"shipping\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-26 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-26 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-26 - persisted order state is shipping', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('shipping');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-27 | Reject admin shipping-to-canceled transition under the explicit state graph.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-27\n\nTechnique / Tag: State transition — Ambiguous edge: Reject admin shipping-to-canceled transition under the explicit state graph\n\nObjective: Reject admin shipping-to-canceled transition under the explicit state graph.\n\nPreconditions: Independent order is shipping.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: JSON invalid-transition error; stored state remains shipping.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"canceled\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\",\"shipping\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-27 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-27 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-27 - persisted order state is shipping', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('shipping');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-28 | Reject delivered to pending.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-28\n\nTechnique / Tag: State transition — Final-state edge: Reject delivered to pending\n\nObjective: Reject delivered to pending.\n\nPreconditions: Independent order is delivered.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Error; delivered remains unchanged.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"pending\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\",\"shipping\",\"delivered\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-28 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-28 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-28 - persisted order state is delivered', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('delivered');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-29 | Reject canceled-to-delivered because canceled is final.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-29\n\nTechnique / Tag: State transition — Final-state edge: Reject canceled-to-delivered because canceled is final\n\nObjective: Reject canceled-to-delivered because canceled is final.\n\nPreconditions: Independent order is canceled.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Invalid-transition JSON; state remains canceled.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"delivered\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  pm.sendRequest({ url: `${base}/api/orders/${orderId}/cancel`, method: 'PUT', header: { 'X-Student-Id': String(sid), Authorization: `Bearer ${ownerToken}` } }, function (cancelErr, cancelRes) { if (cancelErr || cancelRes.code !== 200) fail(`Cancel fixture failed: ${cancelErr || cancelRes.code}`); });",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-29 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-29 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-29 - persisted order state is canceled', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('canceled');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-30 | Reject pending-to-pending as a non-edge state update.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-30\n\nTechnique / Tag: State transition — Same-state/idempotency: Reject pending-to-pending as a non-edge state update\n\nObjective: Reject pending-to-pending as a non-edge state update.\n\nPreconditions: Independent order is pending.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 400\n\nExpected result: Invalid-transition JSON; state remains pending.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"pending\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-30 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-30 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-30 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-34 | Run two concurrent owner cancellations and verify safe final-state integrity.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/checkout",
            "description": "Testcase: TC-B-34\n\nTechnique / Tag: State transition — Concurrency/idempotency: Run two concurrent owner cancellations and verify safe final-state integrity\n\nObjective: Run two concurrent owner cancellations and verify safe final-state integrity.\n\nPreconditions: One own pending order; two parallel user requests use the same ID.\n\nDesign steps: 1. Arrange one owned pending order and record an unrelated order. 2. Prepare two identical cancel requests using the same owner token and order ID. 3. Release both requests concurrently. 4. Record both statuses/bodies. 5. Read back both orders and assert the target is exactly canceled, the unrelated order is unchanged, and no 5xx/corruption occurred.\n\nExpected status: At least one 200; other 200 or 400; never 5xx\n\nExpected result: Final state is exactly canceled; no cross-order mutation/corruption. Response combination is recorded as a reliability observation.\n\nImplementation note: the visible request creates the independent pending fixture. The post-response script releases the two target PUT requests concurrently and verifies the final state.",
            "body": {
              "mode": "raw",
              "raw": "{\"total_amount\":100000,\"shipping_address\":\"HW06 concurrency fixture\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('TC-B-34 - orchestration checkout succeeds', function () { pm.response.to.have.status(200); });",
                  "const orderId = pm.response.json().orderId;",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const userToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const common = { 'X-Student-Id': String(sid) };",
                  "const results = [];",
                  "function complete(label, err, response) {",
                  "  results.push({ label, err, response });",
                  "  if (results.length !== 2) return;",
                  "  pm.test('TC-B-34 - concurrent requests never return 5xx', function () { results.forEach(r => { pm.expect(r.err).to.equal(null); pm.expect(r.response.code).to.be.below(500); }); });",
                  "  pm.test('TC-B-34 - at least one cancellation succeeds', function () {",
                  "    const codes = results.map(r => r.response.code);",
                  "    pm.expect(codes).to.include(200);",
                  "    codes.forEach(code => pm.expect([200, 400]).to.include(code));",
                  "  });",
                  "  pm.sendRequest({ url: `${base}/api/orders/${orderId}`, method: 'GET', header: { ...common, Authorization: `Bearer ${adminToken}` } }, function (verifyErr, verifyRes) {",
                  "    pm.test('TC-B-34 - final concurrent state is exactly canceled', function () { pm.expect(verifyErr).to.equal(null); pm.expect(verifyRes.code).to.eql(200); pm.expect(verifyRes.json().status).to.eql('canceled'); });",
                  "  });",
                  "}",
                  "const cancelRequest = { url: `${base}/api/orders/${orderId}/cancel`, method: 'PUT', header: { ...common, Authorization: `Bearer ${userToken}` } };",
                  "pm.sendRequest(cancelRequest, (err, response) => complete('cancel-1', err, response));",
                  "pm.sendRequest(cancelRequest, (err, response) => complete('cancel-2', err, response));"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-37 | Verify the admin status API rejects the missing backward edge `confirmed → pending`.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-37\n\nTechnique / Tag: State transition — Invalid backward edge: Reject confirmed to pending\n\nObjective: Verify the admin status API rejects the missing backward edge `confirmed → pending`.\n\nPreconditions: An independent order has been moved from pending to confirmed; a valid admin token and read-back request are available.\n\nDesign steps: 1. Arrange an independent confirmed order and record an unrelated order. 2. Send the admin status request once. 3. Assert a JSON invalid-transition response. 4. Read both orders back. 5. Verify the target remains confirmed and the unrelated order is unchanged.\n\nExpected status: 400\n\nExpected result: Invalid-transition JSON; persisted state remains confirmed; no cross-order mutation or internal leakage.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"pending\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-37 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-37 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-37 - persisted order state is confirmed', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('confirmed');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-38 | Verify the admin status API cannot move a delivered final-state order to canceled.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{adminToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-38\n\nTechnique / Tag: State transition — Final-state edge: Reject delivered to canceled\n\nObjective: Verify the admin status API cannot move a delivered final-state order to canceled.\n\nPreconditions: An independent order has completed `pending → confirmed → shipping → delivered`; a valid admin token and read-back request are available.\n\nDesign steps: 1. Arrange and record an independent delivered order. 2. Send the admin status request once. 3. Assert a JSON invalid-transition response. 4. Read the order back. 5. Verify it remains delivered and no cancellation side effect occurred.\n\nExpected status: 400\n\nExpected result: Invalid-transition JSON; delivered remains unchanged because it is final; no refund/cancellation side effect or internal leakage.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"canceled\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [\"confirmed\",\"shipping\",\"delivered\"];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [400];",
                  "pm.test('TC-B-38 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-38 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-38 - persisted order state is delivered', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('delivered');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-39 | Verify a simultaneous `pending → confirmed` admin update and owner `pending/confirmed → canceled` request cannot lose the valid cancellation or corrupt state.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/checkout",
            "description": "Testcase: TC-B-39\n\nTechnique / Tag: State transition — Concurrency/race: Admin confirmation versus owner cancellation\n\nObjective: Verify a simultaneous `pending → confirmed` admin update and owner `pending/confirmed → canceled` request cannot lose the valid cancellation or corrupt state.\n\nPreconditions: One owned order is pending; valid owner/admin tokens are available; two requests can be released through a synchronization barrier; an unrelated order is recorded.\n\nDesign steps: 1. Arrange one pending owned order and record an unrelated order. 2. Prepare the admin confirmation and owner cancellation requests. 3. Release both through the same synchronization barrier. 4. Record both statuses/bodies. 5. Read both orders back. 6. Repeat on fresh fixtures to exercise both interleavings.\n\nExpected status: Owner cancel: 200; admin update: 200 or 400; never 5xx\n\nExpected result: Final target state is exactly canceled for every run; no lost update, illegal state, duplicate side effect, or cross-order mutation occurs. The response combination is recorded for concurrency diagnosis.\n\nImplementation note: the visible request creates the independent pending fixture. The post-response script releases the two target PUT requests concurrently and verifies the final state.",
            "body": {
              "mode": "raw",
              "raw": "{\"total_amount\":100000,\"shipping_address\":\"HW06 concurrency fixture\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('TC-B-39 - orchestration checkout succeeds', function () { pm.response.to.have.status(200); });",
                  "const orderId = pm.response.json().orderId;",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const userToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const common = { 'X-Student-Id': String(sid) };",
                  "const results = [];",
                  "function complete(label, err, response) {",
                  "  results.push({ label, err, response });",
                  "  if (results.length !== 2) return;",
                  "  pm.test('TC-B-39 - concurrent requests never return 5xx', function () { results.forEach(r => { pm.expect(r.err).to.equal(null); pm.expect(r.response.code).to.be.below(500); }); });",
                  "  pm.test('TC-B-39 - owner cancellation succeeds and admin result is safe', function () {",
                  "    const cancel = results.find(r => r.label === 'cancel').response.code;",
                  "    const confirm = results.find(r => r.label === 'confirm').response.code;",
                  "    pm.expect(cancel).to.eql(200);",
                  "    pm.expect([200, 400]).to.include(confirm);",
                  "  });",
                  "  pm.sendRequest({ url: `${base}/api/orders/${orderId}`, method: 'GET', header: { ...common, Authorization: `Bearer ${adminToken}` } }, function (verifyErr, verifyRes) {",
                  "    pm.test('TC-B-39 - final concurrent state is exactly canceled', function () { pm.expect(verifyErr).to.equal(null); pm.expect(verifyRes.code).to.eql(200); pm.expect(verifyRes.json().status).to.eql('canceled'); });",
                  "  });",
                  "}",
                  "pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}`, 'Content-Type': 'application/json' }, body: { mode: 'raw', raw: JSON.stringify({ status: 'confirmed' }) } }, (err, response) => complete('confirm', err, response));",
                  "pm.sendRequest({ url: `${base}/api/orders/${orderId}/cancel`, method: 'PUT', header: { ...common, Authorization: `Bearer ${userToken}` } }, (err, response) => complete('cancel', err, response));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "03 - Security",
      "item": [
        {
          "name": "TC-B-07 | Attempt to cancel user B's pending order with user A token.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-07\n\nTechnique / Tag: Security — BOLA/IDOR\n\nObjective: Attempt to cancel user B's pending order with user A token.\n\nPreconditions: orderIdOtherUser belongs to user2Token and is pending.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 404 under proposed A-010 policy\n\nExpected result: Generic not-found/non-disclosing JSON; no owner/status details; user B's order remains pending."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userBToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [404];",
                  "pm.test('TC-B-07 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-07 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-07 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});",
                  "pm.test('TC-B-07 - IDOR response does not disclose order details', function () { pm.expect(pm.response.text()).not.to.match(/user_id|shipping_address|total_amount|pending|confirmed|shipping|delivered/i); });"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-12 | Call cancel without Authorization.",
          "request": {
            "method": "PUT",
            "header": [],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-12\n\nTechnique / Tag: Security — SEC-02: Authentication/missing\n\nObjective: Call cancel without Authorization.\n\nPreconditions: Own pending order ID exists.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 401\n\nExpected result: Unauthorized JSON; order remains pending."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [401];",
                  "pm.test('TC-B-12 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-12 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-12 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-13 | Call cancel with empty Bearer token.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer ",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-13\n\nTechnique / Tag: Security — SEC-02: Authentication/empty\n\nObjective: Call cancel with empty Bearer token.\n\nPreconditions: Own pending order exists.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 401 or 403\n\nExpected result: Authentication error; order unchanged; no token parsing details."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [401,403];",
                  "pm.test('TC-B-13 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-13 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-13 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-14 | Call cancel with Basic scheme instead of Bearer.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Basic dGVzdDp0ZXN0",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-14\n\nTechnique / Tag: Security — SEC-02: Authentication/scheme\n\nObjective: Call cancel with Basic scheme instead of Bearer.\n\nPreconditions: Own pending order exists.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 401\n\nExpected result: Unauthorized; no state change."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [401];",
                  "pm.test('TC-B-14 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-14 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-14 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-15 | Call cancel with a tampered JWT signature.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tamperedUserToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-15\n\nTechnique / Tag: Security — SEC-02: Authentication/tamper\n\nObjective: Call cancel with a tampered JWT signature.\n\nPreconditions: Own pending order exists; token copy has one signature character changed.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 403\n\nExpected result: Forbidden/authentication error; order remains pending; no secret leakage."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [403];",
                  "pm.test('TC-B-15 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-15 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-15 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-16 | Use normal user token on admin status endpoint.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/admin/orders/{{currentOrderId}}/status",
            "description": "Testcase: TC-B-16\n\nTechnique / Tag: Security — SEC-03: Function-level authorization\n\nObjective: Use normal user token on admin status endpoint.\n\nPreconditions: Independent pending order exists.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 403\n\nExpected result: Role error; stored state remains pending.",
            "body": {
              "mode": "raw",
              "raw": "{\"status\":\"confirmed\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [403];",
                  "pm.test('TC-B-16 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-16 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-16 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-B-36 | Verify that an expired owner JWT cannot cancel an order.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{expiredUserToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-36\n\nTechnique / Tag: Security — SEC-02: Authentication/expired token\n\nObjective: Verify that an expired owner JWT cannot cancel an order.\n\nPreconditions: An own pending order exists; `{{expiredUserToken}}` is correctly signed for the owner but its `exp` time is in the past; a valid token is available for read-back.\n\nDesign steps: 1. Arrange and record an own pending order plus an unrelated order. 2. Send the cancel request with `{{expiredUserToken}}`. 3. Assert the authentication error is generic and JSON. 4. Read both orders with valid credentials. 5. Verify neither order changed and no token claims or validation internals leaked.\n\nExpected status: 403 under the reviewed invalid-token policy; never 2xx\n\nExpected result: Generic authentication error; target remains pending; unrelated order is unchanged; response exposes no JWT payload, signing detail, stack trace, or secret."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200,201,202,204,403];",
                  "pm.test('TC-B-36 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-36 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-36 - persisted order state is pending', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('pending');",
                  "  });",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "04 - Schema validation",
      "item": [
        {
          "name": "TC-B-35 | Validate cancel success contract and persisted postconditions comprehensively.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{userToken}}",
                "type": "text"
              }
            ],
            "url": "{{baseUrl}}/api/orders/{{currentOrderId}}/cancel",
            "description": "Testcase: TC-B-35\n\nTechnique / Tag: Schema validation — SCHEMA-FR10: Schema/postcondition\n\nObjective: Validate cancel success contract and persisted postconditions comprehensively.\n\nPreconditions: Own pending order exists.\n\nDesign steps: 1. Arrange an independent order fixture in the stated starting state. 2. Send the request once. 3. Assert status/body. 4. Read the order back with the owner/admin verification request. 5. Assert exact persisted state and no cross-user mutation.\n\nExpected status: 200\n\nExpected result: 200 application/json; body matches approved success schema; read-back status canceled; owner unchanged; sampled unrelated order unchanged."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.unset('fixtureError');",
                  "const ownerToken = pm.collectionVariables.get('userToken');",
                  "const adminToken = pm.collectionVariables.get('adminToken');",
                  "const sid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const base = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const common = { 'X-Student-Id': String(sid), 'Content-Type': 'application/json' };",
                  "function fail(message) { pm.collectionVariables.set('fixtureError', message); console.error('[HW06 fixture]', message); }",
                  "pm.sendRequest({ url: `${base}/api/checkout`, method: 'POST', header: { ...common, Authorization: `Bearer ${ownerToken}` }, body: { mode: 'raw', raw: JSON.stringify({ total_amount: 100000, shipping_address: 'HW06 fixture' }) } }, function (err, response) {",
                  "  if (err || response.code !== 200) return fail(`Checkout failed: ${err || response.code}`);",
                  "  const orderId = response.json().orderId;",
                  "  pm.collectionVariables.set('currentOrderId', String(orderId));",
                  "  const transitions = [];\n  function next(index) {\n    if (index >= transitions.length) return;\n    pm.sendRequest({ url: `${base}/api/admin/orders/${orderId}/status`, method: 'PUT', header: { ...common, Authorization: `Bearer ${adminToken}` }, body: { mode: 'raw', raw: JSON.stringify({ status: transitions[index] }) } }, function (transitionErr, transitionRes) {\n      if (transitionErr || transitionRes.code !== 200) return fail(`Transition ${transitions[index]} failed: ${transitionErr || transitionRes.code}`);\n      next(index + 1);\n    });\n  }\n  next(0);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const allowedStatus = [200];",
                  "pm.test('TC-B-35 - status follows the reviewed oracle', function () {",
                  "  pm.expect(allowedStatus, `Actual status: ${pm.response.code}`).to.include(pm.response.code);",
                  "});",
                  "pm.test('TC-B-35 - response does not leak server internals', function () {",
                  "  const body = pm.response.text();",
                  "  pm.expect(body).not.to.match(/super_secret|SQLITE_(ERROR|CONSTRAINT)|Database Error|at [A-Za-z0-9_.$]+ \\([^)]*server\\.js:\\d+/i);",
                  "});",
                  "pm.test('FR-10 fixture setup completed', function () { pm.expect(pm.collectionVariables.get('fixtureError') || '').to.eql(''); });",
                  "const verifyBase = pm.environment.get('baseUrl') || pm.collectionVariables.get('baseUrl');",
                  "const verifySid = pm.environment.get('studentId') || pm.collectionVariables.get('studentId');",
                  "const verifyId = pm.collectionVariables.get('currentOrderId');",
                  "pm.sendRequest({ url: `${verifyBase}/api/orders/${verifyId}`, method: 'GET', header: { 'X-Student-Id': String(verifySid), Authorization: `Bearer ${pm.collectionVariables.get('adminToken')}` } }, function (err, response) {",
                  "  pm.test('TC-B-35 - persisted order state is canceled', function () {",
                  "    pm.expect(err).to.equal(null);",
                  "    pm.expect(response.code).to.eql(200);",
                  "    pm.expect(response.json().status).to.eql('canceled');",
                  "  });",
                  "});",
                  "pm.test('TC-B-35 - cancellation success body has the reviewed shape', function () { const body = pm.response.json(); pm.expect(body).to.have.property('message').that.is.a('string'); });"
                ]
              }
            }
          ]
        }
      ]
    }
  ]
}
